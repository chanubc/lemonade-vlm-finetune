# Python
__pycache__/
*.pyc
.venv/
venv/

# 대용량 논문 PDF (외부에서 다시 받을 수 있음)
papers/*.pdf

# 데이터: 원본 표와 split은 스크립트로 재생성 가능
data/raw/
data/splits/*.parquet
data/converted/*.json

# 비디오(약 61GB)와 추출 프레임
data/videos/
data/frames/

# 학습 산출물
out/
outputs/
*.ckpt
*.safetensors

# OS
.DS_Store
Thumbs.db

# ── 조리 위험 PoC: 대용량/재생성 가능 데이터는 제외 ──────────────
# (스크립트로 재생성/재다운로드 가능. 커밋은 코드·config·결과·큐레이션 홀드아웃만)
# 주의: gitignore는 인라인 주석 불가 → 패턴은 반드시 단독 줄.
# 커밋 유지: data/holdout_real/holdout_v0, cooking_normals, *.csv/*.jsonl, sources.json, review_*.jpg
data/holdout_real/videos/
data/holdout_real/frames/
data/holdout_real/fire_reference/
data/holdout_real/danger_candidates/
data/synth/
data/synth_vqa/train.json
data/dfire_step0/
data/cooking_probe/
